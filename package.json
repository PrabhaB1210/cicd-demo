{
  "name": "irctc-cicd-demo",
  "version": "1.0.0",
  "description": "CI/CD demonstration app — IRCTC Ticket Booking API",
  "main": "app/server.js",
  "scripts": {
    "start": "node app/server.js",
    "test:unit": "jest tests/unit.test.js --verbose",
    "test:api": "jest tests/api.test.js --verbose",
    "test:smoke": "jest tests/smoke.test.js --verbose",
    "test": "jest --verbose --runInBand"
  },
  "dependencies": {
    "express": "^4.18.2"
  },
  "devDependencies": {
    "jest": "^29.7.0",
    "supertest": "^6.3.4"
  }
}
